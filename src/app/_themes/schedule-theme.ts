import { createTheme } from "@mui/material";

export const scheduleTheme = createTheme({
  palette: {
    primary: {
      main: "#EA6F13",
    },
    secondary: {
      main: "#EEEEEE",
    },
  },
});
